const NoUserFound = () => {
  return (
    <h1
      style={{
        fontFamily: "sans-serif",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        marginTop: "100px",
      }}
    >
      No user found with username
    </h1>
  );
};

export default NoUserFound;
